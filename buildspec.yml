version: 0.2

phases:
  install:
    runtime_versions:
      python3.7
    commands:
      - sleep 1
  build:
    commands:
      - echo $AWS_ACCESS_KEY_ID
      - ls ~/.aws/
#      - mkdir -p serverless-functions/build/python/lib/python3.7/site-packages
#      - bash deploy.sh